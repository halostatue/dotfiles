{{- $opts := dict
      "personal" (eq (get . "role") "personal")
      "work"     (eq (get . "role") "work")
      "macos"    (and (eq $.chezmoi.os "darwin") (output "sw_vers" "-productVersion"))
      "broken"   (dict
          "openjdk11"   true
          "openjdk17"   true
          "ftgl"        true
      )
-}}

        bomber-scan # security
        certstrap  # security
        chainloop # security
        cosign # security
        gitleaks # security
        lemmeknow # security
        minisign # security
        mkcert # security
        ctpv # display
        flyctl # fly
        fping
        glow
        go-mmv
        goawk
        gojq
        grype # security
        htmd-cli # dev
        hwatch
        hyperfine # dev
        jujutsu # scm
        massren
        mdcat # data
        micro # editor
        miller # data
        mved
        navi
        nomino
        oha # web load
        oxipng # graphics
        pandoc # pandoc
        rage # security
        ranger
        redis # redis
        sfm
        step-cli # security
        stgit # alt-git
        tag # macos
        taplo # toml
        tealdeer
        tnef # odd-data-formats
        trivy # security
        viddy # watch
        vifm # file-manager
        viu
        vv
        watch # watch
        watchexec # watch
        xmlstarlet # data-manipulation

{{- /* Things to consider
    - asciinema
    - agg
    - slides
    - tflint
# add a zizmore port
*/ -}}
