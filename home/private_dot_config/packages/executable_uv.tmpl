#!/usr/bin/env bash
# {{ $personal := eq (get . "role") "personal" }}

# A visualisation tool for Architecture Decision Records
uv tool install adr-viewer
# {{- if $personal }}
# Least privilege AWS IAM Terraformer
uv tool install airiam
# A Python module and command line parser for SPF and DMARC records
uv tool install checkdmarc
# Infrastructure as code static analysis
uv tool install checkov
# AWS IAM Security Assessment tool that identifies violations of least privilege and generates a risk-prioritized HTML report.
uv tool install cloudsplaining
# {{ end -}}
# Convert CSV files into a SQLite database
uv tool install csvs-to-sqlite
# {{- if $personal }}
# Command-line tool and Python library to efficiently diff rows across two different databases.
uv tool install data-diff[postgresql]
# An open source multi-tool for exploring and publishing data
uv tool install datasette
# Tool for detecting secrets in the codebase
uv tool install detect-secrets
# Convert docs to the docset format.
uv tool install doc2dash
# Style checker for Sphinx (or other) RST documentation
uv tool install doc8
# {{ end -}}
# dokter: The doctor for your Dockerfiles
uv tool install dokter
# TUI Ebook Reader
uv tool install epy-reader
# A simple packaging tool for simple packages.
uv tool install flit
# Export Github repository issues to markdown files: https://github.com/mattduck/gh2md.
uv tool install gh2md
# Quickly rewrite git repository history
uv tool install git-filter-repo
# Probably the sharpest git repository organizer & rebase/merge workflow automation tool you've ever seen
uv tool install git-machete
# Determine when a particular commit was merged into a git branch
uv tool install git-when-merged
# Manage multiple git repos with sanity
uv tool install gita
# A utility to diff tree-like files such as JSON and XML.
uv tool install graphtage
# {{- if $personal }}
# The SQL IDE for Your Terminal.
uv tool install harlequin[postgres]
# {{ end -}}
# A way of visualizing a heat map of a git repo
uv tool install heatwave
# {{- if $personal }}
# A Python utility / library to sort Python imports.
uv tool install isort
# {{ end -}}
# Command-line interface to Jinja2 for templating in shell scripts.
uv tool install j2cli
# Converts the output of popular command-line tools and file-types to JSON.
uv tool install jc
# Generate static HTML documentation from JSON schemas
uv tool install json-schema-for-humans
# command line jinja2 template (yet another j2cli)
uv tool install kamidana
# {{- if $personal }}
# CLI for SQLite Databases with auto-completion and syntax highlighting.
uv tool install litecli
# A library of ready-to-use LLM components
uv tool install llm-components
# A high performance build system
uv tool install meson
# Export photos from Apple's macOS Photos app and query the Photos library database to access metadata about images.
uv tool install osxphotos
# Tiny date, time diff calculator with timers
uv tool install pdd
# A modern Python package and dependency manager supporting the latest PEP standards
uv tool install pdm
# CLI for Postgres Database. With auto-completion and syntax highlighting.
uv tool install pgcli
# PostgreSQL Languages AST and statements prettifier
uv tool install pglast
# A command line tool to interact with the PostgreSQL Extension Network.
uv tool install pgxnclient
# A powerful cli tool to interact with the Pieces OS
uv tool install pieces-cli
# Python Development Workflow for Humans.
uv tool install pipenv
# Pipfile to pyproject.toml for Poetry
uv tool install poetrify
# Python dependency management and packaging made easy.
uv tool install poetry
# A Python script and library for analyzing an AWS account's use of IAM.
uv tool install principalmapper
# Pygments is a syntax highlighting package written in Python.
uv tool install pygments
# python code static checker
uv tool install pylint
# {{ end -}}
# Convert between CBOR, JSON, MessagePack, TOML, and YAML
uv tool install remarshal
# reuse is a tool for compliance with the REUSE recommendations.
uv tool install reuse
# {{- if $personal }}
# Pure-Python RSA implementation
uv tool install rsa
# {{ end -}}
# An extremely fast Python linter and code formatter, written in Rust.
uv tool install ruff
# {{- if $personal }}
# Reverse-engineer docker run command line arguments based on running containers
uv tool install runlike
# SAWS: A Supercharged AWS Command Line Interface (CLI)
uv tool install saws
# Easily create lightweight backups of installed packages, dotfiles, and more.
uv tool install shallow-backup
# Slack Export Archive Viewer
uv tool install slack-export-viewer
# The SQL Linter for Humans
uv tool install sqlfluff
# {{ end -}}
# An SSH server & client configuration security auditing tool
uv tool install ssh-audit
# {{- if $personal }}
# Generic Coloriser
uv tool install strec --include-deps
# Singer.io target for writing CSV files
uv tool install target-csv
# Taupe: a tool to extract URLs from your personal Twitter archive
uv tool install taupe
# tox is a generic virtualenv management and test command line tool
uv tool install tox
# Lint tool for Vim script Language
uv tool install vim-vint
# View colored, incremental diff in a workspace or from stdin, in side-by-side or unified moded, and auto paged
uv tool install ydiff
# {{ end -}}
