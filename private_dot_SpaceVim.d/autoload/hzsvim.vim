function! hzsvim#before() abort
  " other configs
  " let g:github_dashboard = { 'username': 'yourgithubuser', 'password': $GITHUB_TOKEN }
  " let g:gista#client#default_username = 'monkeyxite'
endf

function! hzsvim#after() abort
  " do nothing
end
